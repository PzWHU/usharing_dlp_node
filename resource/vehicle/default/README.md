# Vehicle Calibration

Place the target vehicle's SenseAuto sensor calibration files in this
directory, or update `resource/config/dlp_node/<platform>/planning_cfg` to
point to another directory under `usharing_dlp_node/resource`.
